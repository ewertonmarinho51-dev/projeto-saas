# ============================================================
# GovDocs Wizard — Gerador de Documentos da Fase Preparatória
# Lei nº 14.133/2021
#
# Instalação:  pip install -r requirements.txt
# Execução:    streamlit run app.py
# ============================================================

# Interface web (frontend + backend integrados)
#
# FIXADA, não em faixa aberta. Com `>=`, o CI instalava a versão mais
# nova do dia: a 1.64.0 saiu no meio de um PR, mudou o `at.session_state`
# do AppTest para resolver atributo como chave, e uma prova quebrou no CI
# enquanto passava no ambiente local — sem que uma linha do app tivesse
# mudado. O problema não foi a API ter mudado; foi a mudança ter chegado
# sem ninguém decidir.
#
# Para subir: troque o número, rode a suíte inteira, conserte o que
# quebrar e diga no commit o que mudou. `tests/test_dependencia_streamlit.py`
# guarda o teto e também que o ambiente rode a versão fixada aqui.
streamlit==1.64.0

# Integração com a IA — motor principal: OpenAI
openai>=1.40.0

# Integração com a IA — fallback: Google Gemini (SDK oficial atual)
# Obs.: o pacote antigo "google-generativeai" foi descontinuado pelo Google.
google-genai>=1.0.0

# Geração de arquivos .docx
python-docx>=1.1.0

# Geração de arquivos .pdf
fpdf2>=2.7.8

# Banco de dados (Supabase/PostgreSQL) — persistência dos processos
supabase>=2.4.0

# Leitura de PDFs da Base de Conhecimento (RAG)
pypdf>=4.0.0

# Captura visual de identidade (render do PDF-modelo + recorte de imagens)
pymupdf>=1.24.0
pillow>=10.0.0

# Importação da planilha orçamentária de arquivos Excel (.xlsx)
openpyxl>=3.1.0

# Editor visual e exportação: parsing canônico e sanitização local.
markdown-it-py==4.0.0
bleach==6.2.0
